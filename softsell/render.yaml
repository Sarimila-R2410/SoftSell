services:
  - type: web
    name: softsell
    env: node
    rootDir: softsell
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.17.0 